package main

import "fmt"

func main() {
	//config.Info = config.New("./config/config.yaml")
	//engine := gin.Default()
	//routers.Load(engine)
	//if err := engine.Run("0.0.0.0:8080"); err != nil {
	//	log.Fatal(err)
	//}
	for {
		fmt.Println("Starting!")
	}
}
